# Agent model routing

Last updated: 2026-08-10
Owner: coordinator
Status: In Progress

## Available models

- VERIFIED from current agent runtime metadata: `gpt-5.6-sol` is the frontier agentic coding model. Route architecture, cross-boundary reasoning, difficult debugging, security review, Fresh Eyes, Red Team, and integration disputes here.
- VERIFIED from current agent runtime metadata: `gpt-5.6-terra` is the balanced everyday agentic coding model. Route bounded implementation, Docker operation, focused test execution, log classification, and tracker updates here.

Do not use model choice as ownership. Every task still needs one role owner, exact scope, dependencies, acceptance command, and handoff.

## Routing policy

| Task type | Default model | Effort | Use when | Avoid when |
|---|---|---:|---|---|
| `reason` | `gpt-5.6-sol` | `high` | Architecture, interface choice, root-cause analysis across modules | Pure command execution or mechanical edits |
| `resolve` | `gpt-5.6-sol` | `high` or `xhigh` | P0 or P1 defect, data safety, rollback, auth, concurrency | Well-specified low-risk patch |
| `execute` | `gpt-5.6-terra` | `high` | Bounded code change with settled contract and exact tests | Unsettled architecture or destructive recovery design |
| `runtime` | `gpt-5.6-terra` | `medium` or `high` | Docker build, Compose operation, log following, benchmarks, evidence capture | Interpreting a new security or consistency failure alone |
| `review` | `gpt-5.6-sol` | `high` or `xhigh` | Fresh Eyes, Red Team, security, contract and release review | Editing the reviewed files |
| `track` | `gpt-5.6-terra` | `medium` | GitHub read-only refresh, status index, evidence normalization | Making closure or release decisions |

Use `fork_turns: "none"` for fresh-context tasks. Put all needed repository paths, task state, and contracts in the assignment. Use a small positive fork only when the task depends on the last few user decisions and repeating them would risk drift.

## Current Docker release task index

| Task ID | Status | Type | Owner | Model and effort | Scope | Depends on | Linked record | Acceptance |
|---|---|---|---|---|---|---|---|---|
| CITADEL-SEARCH-EMPTY-01 | In Progress | `resolve` | implementer | `gpt-5.6-sol`, `high`. Needs semantic distinction between no data and provider failure. | `kb/cognee_client.py`, focused tests only | None | `BLK-2026-08-09-03` | Dockerized red test fails before fix and passes after fix; empty-dataset `/search` returns HTTP `200` with zero results; `citadel status --check-search` keeps top-level health true and records an honest zero count without a provider exception. The existing zero-result canary remains unavailable by design. |
| CITADEL-BACKUP-GEN-01 | Completed | `resolve` | implementer | `gpt-5.6-sol`, `xhigh`. Restore identity and rollback can destroy or orphan state. | `kb/generation_backup.py`, backup CLI, focused tests | Backup contract CITADEL-INT-BACKUP-01 | `BLK-2026-08-09-02` | Wrong-generation restore fails before writes; failed count verification leaves target Qdrant and filesystem empty |
| CITADEL-BACKUP-CONTAINMENT-01 | Completed | `resolve` | implementer | `gpt-5.6-sol`, `xhigh`. Symlink cleanup and nested destination traversal can remove or contaminate operator state. | `kb/generation_backup.py`, backup CLI, focused backup tests | CITADEL-BACKUP-GEN-01 and approved Docker release design | `BLK-2026-08-09-02` | VERIFIED: Docker containment returned `15 passed`; real successful restore and count-mismatch rollback passed against separate targets; rollback ended with zero collections |
| CITADEL-BACKUP-LIVE-ISOLATION-01 | Completed | `resolve` | runtime | `gpt-5.6-terra`, `high`. The successful live restore intentionally retains collections for evidence, so the rollback test needs a separate empty target. | `.github/workflows/test.yml` and disposable Docker topology only; live test source remains unchanged | Reproduced exact-file Docker order failure after one successful restore; cleanup-code safety guard | `BLK-2026-08-09-02` | VERIFIED: success restore retained six collections; separate rollback target started and ended empty; source, success, and rollback logs had zero severe and zero non-2xx lines |
| CITADEL-DOCKER-TEST-01 | Blocked | `execute` | runtime | `gpt-5.6-terra`, `high`. Corrected image exists, but host capacity is below the declared runtime floor. | Exclusive Docker mutation; reuse image `sha256:7de749c08a5ab6d760a66652f1e0e5f35b5e8f7e7f702be58a5b63841c2e6d39` | Source commits pushed to draft PR 256; explicit cache-prune approval pending | `BLK-2026-08-10-03` | Non-live suite, Ruff, three live Qdrant tests, and production Compose run from the corrected image with classified logs and zero unexpected failures |
| CITADEL-DOCKER-NONLIVE-FAILURES-01 | Completed | `resolve` | reviewer | `gpt-5.6-sol`, `high`. First final-image non-live run exposed three failures but ephemeral output lost their identities. | Retained image `citadel-codex-20260810-test`; read-only source diagnosis after retained-container reproduction | CITADEL-DOCKER-TEST-01 Phase 1 failure on 2026-08-10 | `BLK-2026-08-09-04` | VERIFIED: retained rerun returned three exact failures and proved relevant image files matched the worktree byte-for-byte |
| CITADEL-CANARY-TIMEOUT-CONTRACT-01 | Completed | `resolve` | implementer | `gpt-5.6-terra`, `high`. Bounded stale assertion after typed timeout behavior landed. | `tests/test_agent_canary.py` only | CITADEL-RETRIEVAL-ERRORS-01 | CITADEL-INT-RETRIEVAL-01 | VERIFIED: canary timeout regression expects exit `1`, `SEARCH_TIMEOUT`, and HTTP `504`; root combined test passed |
| CITADEL-PUBLISH-TAG-ASSERTION-01 | Completed | `resolve` | implementer | `gpt-5.6-terra`, `high`. Bounded test false positive confuses the GitHub runner label with a mutable image tag. | `tests/test_publish_workflow.py` only | CITADEL-OCI-RELEASE-IMPL-01 | Immutable OCI release contract | VERIFIED: test rejects literal mutable OCI `:latest` tag while allowing `runs-on: ubuntu-latest`; root combined test passed |
| CITADEL-SHARE-AUDIT-ACCEPTED-01 | Completed | `resolve` | implementer | `gpt-5.6-terra`, `high`. Final retry success is returned and audited through MCP but omitted from the persisted access event. | `kb/server.py` only | CITADEL-RECEIPT-EXPOSURE-01 | CITADEL-INT-LIFECYCLE-01 | VERIFIED: successful persisted share event records final `accepted`; root combined retry regression passed |
| CITADEL-DOCKER-NONROOT-COGNEE-01 | In Progress | `resolve` | implementer | `gpt-5.6-sol`, `high`. A non-root Docker failure exposed Cognee package-directory defaults plus a missing Lite cache-root binding. | `kb/lite_runtime.py`, `tests/test_lite_runtime.py`, focused `tests/test_cognee_client.py` hunk; Dockerfile remains owned by Docker operator | Reproduced Docker failure at 279 passed and CITADEL-INT-SELFHOST-01 cache-root update | `BLK-2026-08-09-04` | Exact session test passes as UID 10001; all mutable Cognee roots resolve below `/data`; read-only production root plus writable `/data` supports cache and prune initialization without package writes |
| CITADEL-COMPOSE-LLM-PREFLIGHT-01 | Blocked | `resolve` | implementer | `gpt-5.6-terra`, `high`. Offline Compose readiness used a dummy key but Cognee attempted an external model authentication check. | Both Compose files, `.env.lite.example`, focused deploy and Docker workflow tests | Reproduced production Compose log failure and CITADEL-INT-SELFHOST-01 update | `BLK-2026-08-09-06` | Compose defaults skip flag false; explicit CI true reaches container; offline readiness logs have no model-auth error; later seeded gates do not set the skip flag |
| CITADEL-COMPOSE-READYZ-MODEL-01 | In Progress | `resolve` | implementer | `gpt-5.6-sol`, `high`. Clean production boot triggers a real model path after Uvicorn startup, so the exact caller and readiness boundary required deep tracing before implementation. | `kb/server.py`, focused `tests/test_server.py`; Docker runtime remains separately owned | Failed recovered production Compose gate and DEC-2026-08-09-03 | `BLK-2026-08-09-06`; CITADEL-INT-SELFHOST-01 | Source-reverted Docker test fails with nonzero collection; current focus passes; fresh Compose returns readiness 200 with zero lifecycle sources and zero relational documents, no model call, and clean logs |
| CITADEL-HOST-RECOVERY-01 | Completed | `execute` | release | Root-owned destructive confirmation gate and Docker resource preservation. | UV cache, Docker Desktop process state, BuildKit cache; no repository source | Explicit user approval; preserved final3 resource inventory | `BLK-2026-08-09-05` | UV cache absent; Docker Server responds; final3 healthy with six collections; images, containers, and volumes preserved; host has multiple GiB free |
| CITADEL-RING0-INTEGRATION-AUDIT-01 | Completed | `review` | reviewer | `gpt-5.6-sol`, `high`. The shared worktree has 22 modified tracked files and six untracked files spanning runtime, backup, retrieval, and Docker boundaries. | Read-only review of the complete `/private/tmp/citadel-v050-qdrant` diff | Ring 0 implementer handoffs and current contracts | CITADEL-INT-SELFHOST-01, CITADEL-INT-QDRANT-01, CITADEL-INT-WHOLE-GENERATION-01 | VERIFIED by root source reproduction: no P0; four P1 gaps recorded under BLK-2026-08-09-02 and BLK-2026-08-09-07 through `-09`; no files changed; `git diff --check HEAD` clean |
| CITADEL-BACKUP-RESTORE-LOCK-01 | In Progress | `resolve` | implementer | `gpt-5.6-sol`, `high`. Cleanup ownership across a lost lock race is a data-loss boundary. | `kb/generation_backup.py`, `tests/test_generation_backup.py` only | Root source reproduction of restore lock race | `BLK-2026-08-09-07`; CITADEL-INT-BACKUP-01 | Docker red regression loses the lock after the empty-target check and preserves the winning runtime's state; green focused backup suite passes |
| CITADEL-DOCKER-LOG-ROOTFS-01 | In Progress | `resolve` | implementer | `gpt-5.6-terra`, `high`. Both fixes are bounded Docker contract plumbing in shared Compose/workflow tests. | `.github/workflows/test.yml`, both Compose files, `tests/test_docker_workflow.py` only | Root source reproduction of classifier and rootfs gaps | `BLK-2026-08-09-08`, `BLK-2026-08-09-09`; CITADEL-INT-SELFHOST-01 | Structural tests reject missed warning/failure lines and missing read-only root; Docker production boot runs UID 10001 with read-only root and writable `/data` plus `/tmp`; classified logs pass declared phase rules |
| CITADEL-LADYBUG-WRITABLE-HOME-01 | Completed | `resolve` | researcher | `gpt-5.6-sol`, `high`. Provider extension-path behavior under a read-only root must be traced against exact installed Ladybug and Cognee source before changing runtime environment. | Read-only Ladybug/Cognee source, `kb/lite_runtime.py`, both Compose files, focused Lite/Docker tests, failed Compose log | Read-only-root Compose failure | `BLK-2026-08-10-01`; CITADEL-INT-SELFHOST-01 | VERIFIED: exact Ladybug `0.18.1` supports per-connection `home_directory`; exact Cognee `1.4.1` omits it on throwaway, direct, and subprocess paths; no files or Docker resources changed |
| CITADEL-LADYBUG-HOME-PATCH-01 | Completed | `resolve` | implementer | `gpt-5.6-sol`, `high`. Exact-source Cognee wheel patch spans throwaway, direct, subprocess, and replayed Ladybug connections. | `scripts/build_secure_cognee.py`, `kb/lite_runtime.py`, `tests/test_build_secure_cognee.py`, `tests/test_lite_runtime.py` | CITADEL-LADYBUG-WRITABLE-HOME-01 and DEC-2026-08-10-01 | `BLK-2026-08-10-01`; CITADEL-INT-SELFHOST-01 | REPORTED static: Ruff passed, diff check clean, exact patched source compiled; Docker hash, red, and runtime proof delegated separately |
| CITADEL-LADYBUG-DOCKER-GATE-01 | In Progress | `execute` | runtime | `gpt-5.6-terra`, `high`. Bounded image build, source-reverted test, production Compose, restart, filesystem, and log evidence. | Exclusive Docker mutation; current worktree | CITADEL-LADYBUG-HOME-PATCH-01 | `BLK-2026-08-10-01`; CITADEL-INT-SELFHOST-01 | VERIFIED recovery: Docker returned after approved scoped cleanup; final3 is healthy with six Qdrant collections and `10GiB` host space; rerun the source-reverted, focused, full, and Compose gates |
| CITADEL-LADYBUG-PIN-01 | Completed | `resolve` | implementer | `gpt-5.6-terra`, `high`. Clean builds exposed transitive Ladybug patch drift, and the bounded fix is exact packaging pins plus structural tests. | `pyproject.toml`, `requirements.txt`, `Dockerfile`, `tests/test_docker_workflow.py` | DEC-2026-08-10-01 correction | `BLK-2026-08-10-01`; CITADEL-INT-SELFHOST-01 | VERIFIED static: all install surfaces pin `ladybug==0.18.2`; production assertion binds Cognee 1.4.1, Ladybug 0.18.2, Qdrant client 1.19.0; Ruff, diff check, and in-memory syntax passed; Docker verification is owned by CITADEL-LADYBUG-DOCKER-GATE-01 |
| CITADEL-DOCKER-RUNTIME-01 | Planned | `runtime` | runtime | `gpt-5.6-terra`, `high`. Long bounded Compose and evidence run. | Disposable Docker resources and evidence only | Prior three tasks integrated | Docker handoff gate 4 | Production Compose passes auth, ingest, receipts, search, census, CLI, MCP, capture, restart, outage, restore, stress, resource, security, and classified-log gates |
| CITADEL-FRESH-EYES-CORROBORATE-01 | Planned | `review` | reviewer | `gpt-5.6-sol`, `high`. Independent evidence-support review. | Immutable integrated diff, contracts, Docker evidence. Read-only | Runtime and benchmark gates green | Fresh Eyes Corroborate | Supported claims returned with exact evidence and blind spots; root reproduces P0 and P1 findings |
| CITADEL-FRESH-EYES-REFUTE-01 | Planned | `review` | reviewer | `gpt-5.6-sol`, `high`. Independent falsification review. | Same immutable candidate and evidence. Read-only | Runtime and benchmark gates green | Fresh Eyes Refute | Attempted falsifications returned with exact evidence; root reproduces P0 and P1 findings |
| CITADEL-RED-TEAM-01 | Planned | `review` | reviewer | `gpt-5.6-sol`, `xhigh`. Adversarial data-loss, isolation, auth, restore, and concurrency review. | Integrated diff and final Docker evidence. Read-only | Fresh Eyes fixes integrated | Red Team skill | No unresolved reproduced P0 or P1; every accepted finding has Docker regression evidence |
| CITADEL-GITHUB-REFRESH-01 | Planned | `track` | release | `gpt-5.6-terra`, `medium`. Read-only tracker normalization after evidence changes. | GitHub issues and PRs, `.local-review/GITHUB_INDEX.md`, `status.md` | Reviews green and local commits exist | Issues 128, 228, 247; PRs 254, 255, 256 | Exact live counts, heads, checks, dispositions, blind spots, and correction entries recorded. No external write |
| CITADEL-RELEASE-PATH-AUDIT-01 | Completed | `track` | release | `gpt-5.6-terra`, `medium`. Shipping approval is explicit, but exact merge, tag, package, and OCI mechanisms must be read before any external write. | Read-only local workflows, GitHub releases, packages, branch protection, PR 256 | User production-release approval on 2026-08-10 | Release confirmation gate | REPORTED: PR 256 is draft and stale; strict required checks are CI gate, DCO, and gitleaks; current tag workflow publishes PyPI and GitHub Release only; no Citadel OCI package or workflow was determinable; no external write occurred |
| CITADEL-OCI-RELEASE-DESIGN-01 | Completed | `reason` | reviewer | `gpt-5.6-sol`, `high`. Repository has no OCI publication path, while v0.5 requires an immutable Docker release with supply-chain evidence. | Read-only Dockerfile, existing release workflows, GitHub Actions permissions and GHCR conventions | CITADEL-RELEASE-PATH-AUDIT-01 | CITADEL-INT-SELFHOST-01 and production-release approval | REPORTED: one exact-version GHCR index, staged by source SHA, both platforms, max provenance, SBOM, GitHub attestation, digest receipt, no mutable aliases; no files or external state changed |
| CITADEL-OCI-RELEASE-IMPL-01 | Completed | `execute` | implementer | `gpt-5.6-sol`, `high`. Tag publication is irreversible and must fail closed across Python and multi-platform OCI outputs. | `.github/workflows/publish.yml`, `tests/test_publish_workflow.py`, `PUBLISHING.md` | DEC-2026-08-10-02, CITADEL-OCI-RELEASE-DESIGN-01, integrated Docker gate, and user shipping direction on 2026-08-10 | CITADEL-INT-SELFHOST-01 and release confirmation gate | VERIFIED static by root: staged SHA index, both-platform digest smokes, SBOM, maximum-mode provenance, GitHub attestation, exact-version copy, PyPI ordering, and final GitHub Release are encoded; no workflow or external write ran |
| CITADEL-GITHUB-AUDIT-01 | Completed | `track` | release | `gpt-5.6-terra`, `medium`. Live issue and PR classification is bounded read-only tracker work. | GitHub issues and pull requests; `.local-review/GITHUB_INDEX.md` | Revised Docker release design input | User GitHub lane direction on 2026-08-09 | Every open issue and pull request classified as v0.5 blocking, future-valid, superseded, duplicate, or obsolete with live URL, state, head/check evidence, and no external write |
| CITADEL-PLAN-RING0-01 | Completed | `reason` | researcher | `gpt-5.6-sol`, `high`. Four release blockers cross backup, Docker, stress, and search boundaries. | Read-only mapping of Ring 0 source and tests | Approved Docker release design | Design sections 5 and 6; BLK-2026-08-09-02, `-03`, `-04` | REPORTED handoff returned exact boundaries, Docker commands, commit sequence, status-canary correction, and prerequisites; no files changed |
| CITADEL-PLAN-RUNTIME-01 | Completed | `reason` | researcher | `gpt-5.6-sol`, `high`. Compose runtime proof spans lifecycle, all surfaces, outage, restart, and restore. | Read-only mapping of Rings 1 and 2 runtime paths | Approved Docker release design and Ring 0 interfaces | Design sections 3 through 6 | REPORTED handoff returned seed arithmetic, typed-error conflict, receipt gap, snapshot gap, commands, and evidence shape; no files changed |
| CITADEL-PLAN-CORPUS-01 | Completed | `reason` | researcher | `gpt-5.6-sol`, `high`. GitHub, seats, traces, promotion, and benchmark need comparable identities and strict visibility gates. | Read-only mapping of Rings 3 and 4 | Approved Docker release design and runtime seed contract | Design sections 5 through 9 | REPORTED handoff returned exact files, Railway allowlist, current-head evidence, isolation matrix, promotion gap, and benchmark gaps; no files changed |
| CITADEL-STRESS-429-01 | In Progress | `execute` | implementer | `gpt-5.6-terra`, `high`. One bounded test and predicate change. | `scripts/stress_qdrant_search.py`, focused test | Docker test target available | Ring 0 stress gate | Docker red returns `assert 0 == 1`; green rejects all-429 and preserves mixed 200/429 acceptance |
| CITADEL-RETRIEVAL-ERRORS-01 | In Progress | `resolve` | implementer | `gpt-5.6-sol`, `high`. Timeout and provider failures currently conflict with retrieval contracts across three surfaces. | `kb/server.py`, `kb/cli.py`, `kb/mcp_server.py`, `kb/status.py`, focused adapter and tests | Ring 0 search commit and architect contract update completed 2026-08-10 | CITADEL-INT-RETRIEVAL-01 | HTTP 504 `SEARCH_TIMEOUT`; HTTP 503 `QDRANT_UNAVAILABLE`; CLI nonzero, status typed, and MCP `isError=true` with same code; healthy empty remains 200 |
| CITADEL-RECEIPT-EXPOSURE-01 | In Progress | `execute` | implementer | `gpt-5.6-terra`, `high`. Response fields and source data already exist. | `kb/server.py`, `kb/cli.py`, focused share, capture, MCP tests | Source implementation root-reviewed and statically verified 2026-08-10; Docker acceptance pending | CITADEL-INT-LIFECYCLE-01 | Share returns one operation identity per final target outcome, including bounded failure detail; capture JSON preserves source, job, and state without inventing IDs |
| CITADEL-COMPOSE-SNAPSHOT-01 | Completed | `execute` | implementer | `gpt-5.6-terra`, `high`. Bounded Compose storage change. | Both Compose files and contract tests | Ring 0 Docker target | CITADEL-INT-BACKUP-01 | VERIFIED static by root: both Compose files are byte-identical, define distinct `/qdrant/storage` and `/qdrant/snapshots` named volumes plus the pinned snapshot path, Ruff passed, Python compiled, and diff check was clean; Docker snapshot proof remains in the integrated runtime gate |
| CITADEL-RELEASE-PROBE-01 | Completed | `execute` | implementer | `gpt-5.6-terra`, `high`. Versioned fixture and mechanical evidence tooling. | Seed fixture, `kb/release_acceptance.py`, runtime probe, log classifier, focused tests | Typed errors, receipts, current-head evidence, snapshot, runtime hardening source settled | Runtime plan Tasks 5 and 6 | VERIFIED: eight-source manifest binds real runtime keys, exact `3/3/1/1` dataset census, Central identity, and redacted evidence; root focused and negative checks passed |
| CITADEL-GITHUB-INPUTS-01 | Planned | `execute` | implementer | `gpt-5.6-terra`, `high`. Bounded optional environment passthrough. | Both Compose files, `.env.lite.example`, local deploy and tests | Snapshot Compose task | Approved Railway allowlist | Only optional `CITADEL_GITHUB_TOKEN` is copied; unknown env stays out; generated env remains 0600 |
| CITADEL-GITHUB-SMOKE-01 | Blocked | `runtime` | runtime | `gpt-5.6-terra`, `high`. User requested the next live local check and the operation must stay inside one app process. | Preserved disposable Citadel and Qdrant containers; no source edits | Explicit current-message approval for brief local app interruption and authenticated private-repository reads | User direction on 2026-08-09 | Stop the preserved app, run bounded authenticated GitHub and repo-content dry-runs through the exact image, conditionally sync local Citadel data, restart healthy, classify both logs, and remove secret files |
| CITADEL-CURRENT-HEAD-EVIDENCE-01 | In Progress | `resolve` | implementer | `gpt-5.6-sol`, `high`. Current-head joins must reject historical searchable projections. | `kb/lifecycle.py`, `kb/service.py`, `tests/test_current_head_evidence.py` | Architect lifecycle contract; source implementation root-reviewed and statically verified 2026-08-10 | CITADEL-INT-LIFECYCLE-01 | Every frozen GitHub source key resolves to the active head, current generation, one job, and three searchable receipts; source-reverted Docker red and focused green remain pending |
| CITADEL-PROMOTION-APPROVAL-01 | Planned | `resolve` | implementer | `gpt-5.6-sol`, `xhigh`. Current known-org path can write Central without pending approval. | Promotion engine, attestation transport if needed, focused tests | Isolation green and architect promotion contract | Ring 4 promotion invariant | Every qualifying proposal queues; only admin approval writes Central with pending ID, source hash, approver, and time |
| CITADEL-BENCH-RELEASE-01 | Planned | `reason` | implementer | `gpt-5.6-sol`, `high`. Comparison identity and quality versus latency require careful separation. | Retrieval evaluator, benchmark tests, release acceptance | Immutable corpus and retrieval profile contract | Approved benchmark decision | Release mode rejects identity drift, hidden partials, underfill, visibility errors, lost exact answers, and p95 above 20 percent |
| CITADEL-OBSERVED-OUTCOMES-01 | Completed | `review` | reviewer | `gpt-5.6-sol`, `high`. PR 245 predates current architecture and must be reconciled semantically. | Read-only PR 245 behavior inventory against the integrated worktree | Rings 0 through 4 implementation assembled | PR 245 and PR 256 | REPORTED: all PR behaviors classified, no P0, five current P1 truthfulness gaps, no wholesale merge; root reproduction and bounded ports remain separate tasks |
| CITADEL-MESH-TRUTH-01 | Completed | `resolve` | implementer | `gpt-5.6-terra`, `high`. Mesh state mapping is a bounded lifecycle truthfulness fix. | `kb/mesh.py`, `tests/test_mesh.py` | CITADEL-OBSERVED-OUTCOMES-01; CITADEL-INT-LIFECYCLE-01 | PR 245 behavior port; release truthfulness gate | VERIFIED static by root: queued and pending lifecycle writes cannot emit indexed graph evidence; searchable and legacy cases remain explicit; Docker red and green remain pending |
| CITADEL-CAPTURE-RECEIPT-TRUTH-01 | In Progress | `resolve` | implementer | `gpt-5.6-sol`, `high`. Capture success and error receipts span several early exits and must remain secret-safe. | `kb/hooks/sync_push.py`, `kb/hooks/sync_session.py`, focused hook tests | CITADEL-OBSERVED-OUTCOMES-01 | PR 245 behavior port; release truthfulness gate | Source-reverted Docker regressions fail; current focused Docker tests prove only explicit acceptance reports success and every requested-capture skip/error writes one typed bounded receipt |

## Dependency path

```text
CITADEL-SEARCH-EMPTY-01 ----+
                            +--> CITADEL-DOCKER-TEST-01 --> CITADEL-DOCKER-RUNTIME-01
CITADEL-BACKUP-GEN-01 -----+                                      |
                                                                  v
                                                   CITADEL-FRESH-EYES-01
                                                                  |
                                                                  v
                                                    CITADEL-RED-TEAM-01
                                                                  |
                                                                  v
                                                 local commits and tracker refresh
```

Root may run the first two implementers in parallel because their file scopes are disjoint. Docker test infrastructure starts after both red-test contracts are known. Only the runtime owner mutates containers. Reviews start after the integrated Docker gate is green.

## Spawn examples

Deep defect resolution:

```json
{
  "task_name": "backup_generation",
  "fork_turns": "none",
  "model": "gpt-5.6-sol",
  "reasoning_effort": "xhigh",
  "message": "Read the active handoff and BLK-2026-08-09-02. Own only the listed backup files. Add wrong-generation red test first. Run acceptance inside Docker. Return the repository handoff contract."
}
```

Bounded Docker execution:

```json
{
  "task_name": "docker_runtime",
  "fork_turns": "none",
  "model": "gpt-5.6-terra",
  "reasoning_effort": "high",
  "message": "Read the active handoff and CITADEL-DOCKER-RUNTIME-01. Own Docker mutations and both log followers. Do not edit source. Return exact commands, results, classified logs, resource samples, blind spots, and preserved resources."
}
```
