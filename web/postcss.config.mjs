/** Tailwind v4 is a PostCSS plugin; the theme itself lives in
 *  src/styles/globals.css, not in a JS config. */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
